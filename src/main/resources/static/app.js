const api = '';
const state = { token: localStorage.getItem('token'), user: JSON.parse(localStorage.getItem('user') || 'null'), categories: [], products: [], selectedCategory: '', latestOrderId: localStorage.getItem('latestOrderId') || null, authMode: 'login' };
const $ = (id) => document.getElementById(id);
const money = (value) => 'Rs ' + Number(value || 0).toLocaleString('en-IN', { minimumFractionDigits: 2, maximumFractionDigits: 2 });
const escapeHtml = (value) => String(value || '').replace(/[&<>"']/g, (m) => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#039;' }[m]));
const imageFor = (product) => ({ 'AUD-1001': 'https://images.unsplash.com/photo-1505740420928-5e560c06d30e?auto=format&fit=crop&w=700&q=80', 'WCH-2201': 'https://images.unsplash.com/photo-1523275335684-37898b6baf30?auto=format&fit=crop&w=700&q=80', 'BAG-4100': 'https://images.unsplash.com/photo-1553062407-98eeb64c6a62?auto=format&fit=crop&w=700&q=80' }[product.sku] || 'https://images.unsplash.com/photo-1472851294608-062f824d29cc?auto=format&fit=crop&w=700&q=80');

async function request(path, options = {}) {
  const headers = { ...(options.headers || {}) };
  const protectedPath = path.startsWith('/api/cart') || path.startsWith('/api/orders') || path.startsWith('/api/payments') || path.startsWith('/api/admin');
  if (options.body && !headers['Content-Type']) headers['Content-Type'] = 'application/json';
  if (state.token && protectedPath) headers.Authorization = 'Bearer ' + state.token;
  const response = await fetch(api + path, { ...options, headers });
  if (!response.ok) {
    let message = 'Request failed';
    try { const error = await response.json(); message = (error.details && error.details[0]) || error.error || message; } catch (_) {}
    if (response.status === 401) { logout(); message = message || 'Session expired. Please sign in again.'; }
    throw new Error(message);
  }
  const text = await response.text();
  return text ? JSON.parse(text) : null;
}
function toast(message) { const node = $('toast'); node.textContent = message; node.classList.add('show'); setTimeout(() => node.classList.remove('show'), 2600); }
function showError(error) { toast(error.message || 'Something went wrong'); }
function isStrongPassword(password) { return /^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)(?=.*[^A-Za-z0-9]).{8,}$/.test(password || ''); }
function setPasswordHelp(valid) { const help = $('passwordHelp'); if (help) help.classList.toggle('invalid', !valid); }
function saveSession(auth) { state.token = auth.token; state.user = { id: auth.userId, name: auth.name, email: auth.email, role: auth.role }; localStorage.setItem('token', state.token); localStorage.setItem('user', JSON.stringify(state.user)); renderAuth(); }
function logout() { state.token = null; state.user = null; localStorage.removeItem('token'); localStorage.removeItem('user'); renderAuth(); renderCart({ items: [], total: 0 }); toast('Signed out'); }
function renderAuth() { $('authButton').textContent = state.user ? state.user.name + ' / Sign out' : 'Sign in'; $('adminTab').style.display = state.user && state.user.role === 'ADMIN' ? 'inline-flex' : 'none'; }
function setView(view) { $('shopView').classList.toggle('hidden', view !== 'shop'); $('adminView').classList.toggle('hidden', view !== 'admin'); $('catalogTab').classList.toggle('active', view === 'shop'); $('adminTab').classList.toggle('active', view === 'admin'); if (view === 'admin') loadAdmin().catch(showError); }

async function loadCatalog() { state.categories = await request('/api/categories'); state.products = await request('/api/products' + (state.selectedCategory ? '?category=' + encodeURIComponent(state.selectedCategory) : '')); renderCategories(); renderProducts(state.products); fillCategorySelect(); }
function renderCategories() { const list = $('categoryList'); list.innerHTML = ''; const all = document.createElement('button'); all.textContent = 'All products'; all.className = state.selectedCategory ? '' : 'active'; all.onclick = () => { state.selectedCategory = ''; loadCatalog().catch(showError); }; list.appendChild(all); state.categories.forEach((category) => { const btn = document.createElement('button'); btn.textContent = category.name; btn.className = state.selectedCategory === category.slug ? 'active' : ''; btn.onclick = () => { state.selectedCategory = category.slug; loadCatalog().catch(showError); }; list.appendChild(btn); }); }
function renderProducts(products) { $('catalogSummary').textContent = products.length + ' product' + (products.length === 1 ? '' : 's') + ' available'; const grid = $('productGrid'); if (!products.length) { grid.innerHTML = '<div class="empty-state">No products found</div>'; return; } grid.innerHTML = products.map((p) => '<article class="product-card"><div class="product-image" style="background-image:url(' + imageFor(p) + ')"><span class="stock-pill">' + p.stockQuantity + ' in stock</span></div><div class="product-body"><div><h3>' + escapeHtml(p.name) + '</h3><p class="product-meta">' + escapeHtml(p.description || p.category.name) + '</p></div><div class="price-row"><span class="price">' + money(p.price) + '</span><button class="primary" data-add="' + p.id + '">Add</button></div></div></article>').join(''); grid.querySelectorAll('[data-add]').forEach((button) => button.onclick = () => addToCart(button.dataset.add).catch(showError)); }

async function addToCart(productId) { if (!state.token) return openAuth(); const cart = await request('/api/cart/items', { method: 'POST', body: JSON.stringify({ productId: Number(productId), quantity: 1 }) }); renderCart(cart); await loadCatalog(); toast('Added to cart and stock reserved'); }
async function loadCart() { if (!state.token) return renderCart({ items: [], total: 0 }); renderCart(await request('/api/cart')); }
function renderCart(cart) { $('cartCount').textContent = cart.items.reduce((sum, item) => sum + item.quantity, 0); $('cartTotal').textContent = money(cart.total); $('cartItems').innerHTML = cart.items.length ? cart.items.map((item) => '<div class="cart-row"><div class="row-between"><strong>' + escapeHtml(item.productName) + '</strong><span>' + money(item.subtotal) + '</span></div><div class="row-between"><span>' + money(item.unitPrice) + ' x ' + item.quantity + '</span><button class="secondary" data-remove="' + item.id + '">Remove</button></div></div>').join('') : '<div class="empty-state">Your cart is empty</div>'; $('cartItems').querySelectorAll('[data-remove]').forEach((button) => button.onclick = async () => { await request('/api/cart/items/' + button.dataset.remove, { method: 'DELETE' }); await loadCart(); await loadCatalog(); toast('Removed from cart and stock restored'); }); }
async function checkout(event) { event.preventDefault(); if (!state.token) return openAuth(); const shippingAddress = $('shippingAddress').value.trim(); const order = await request('/api/orders/checkout', { method: 'POST', body: JSON.stringify({ shippingAddress }) }); state.latestOrderId = order.id; localStorage.setItem('latestOrderId', order.id); $('shippingAddress').value = ''; await loadCart(); await loadCatalog(); toast('Order #' + order.id + ' created'); }
async function payLatest() { if (!state.latestOrderId) return toast('Checkout first, then pay'); const provider = $('paymentProvider').value; const payment = await request('/api/payments', { method: 'POST', body: JSON.stringify({ orderId: Number(state.latestOrderId), provider }) }); toast('Payment success: ' + payment.transactionId.slice(0, 18)); }

function openAuth() { $('authModal').classList.remove('hidden'); }
function closeAuth() { $('authModal').classList.add('hidden'); }
function setAuthMode(mode) { state.authMode = mode; const register = mode === 'register'; $('nameField').classList.toggle('hidden', !register); if ($('passwordHelp')) $('passwordHelp').classList.toggle('hidden', !register); if ($('passwordField')) $('passwordField').placeholder = register ? 'Strong password' : 'Password'; setPasswordHelp(true); $('authTitle').textContent = register ? 'Create account' : 'Sign in'; $('authSubmit').textContent = register ? 'Create account' : 'Sign in'; $('toggleAuth').textContent = register ? 'I already have an account' : 'Create a new account'; }
async function submitAuth(event) { event.preventDefault(); const payload = Object.fromEntries(new FormData(event.currentTarget).entries()); if (state.authMode === 'login') { delete payload.name; } else if (!isStrongPassword(payload.password)) { setPasswordHelp(false); toast('Please create a stronger password'); return; } const auth = await request('/api/auth/' + (state.authMode === 'register' ? 'register' : 'login'), { method: 'POST', body: JSON.stringify(payload) }); saveSession(auth); closeAuth(); await loadCart(); toast('Welcome, ' + auth.name); }
function fillCategorySelect() { $('productCategorySelect').innerHTML = state.categories.map((c) => '<option value="' + c.id + '">' + escapeHtml(c.name) + '</option>').join(''); }
async function loadAdmin() { if (!state.user || state.user.role !== 'ADMIN') return toast('Admin login required'); const result = await Promise.all([request('/api/admin/dashboard'), request('/api/admin/orders')]); const dashboard = result[0]; const orders = result[1]; $('metrics').innerHTML = [['Users', dashboard.users], ['Products', dashboard.products], ['Orders', dashboard.orders], ['Payments', dashboard.payments]].map(([label, value]) => '<div class="metric"><span>' + label + '</span><strong>' + value + '</strong></div>').join(''); $('adminOrders').innerHTML = orders.length ? orders.map((order) => '<div class="order-row"><div class="row-between"><strong>Order #' + order.id + '</strong><span>' + order.status + '</span></div><div class="row-between"><span>' + money(order.totalAmount) + '</span><select data-order="' + order.id + '">' + ['PENDING','PAID','SHIPPED','DELIVERED','CANCELLED'].map((s) => '<option ' + (s === order.status ? 'selected' : '') + '>' + s + '</option>').join('') + '</select></div></div>').join('') : '<div class="empty-state">No orders yet</div>'; $('adminOrders').querySelectorAll('[data-order]').forEach((select) => select.onchange = async () => { await request('/api/admin/orders/' + select.dataset.order + '/status', { method: 'PATCH', body: JSON.stringify({ status: select.value }) }); toast('Order updated'); }); }
async function createCategory(event) { event.preventDefault(); const payload = Object.fromEntries(new FormData(event.currentTarget).entries()); await request('/api/admin/categories', { method: 'POST', body: JSON.stringify(payload) }); event.currentTarget.reset(); await loadCatalog(); await loadAdmin(); toast('Category created'); }
async function createProduct(event) { event.preventDefault(); const payload = Object.fromEntries(new FormData(event.currentTarget).entries()); payload.price = Number(payload.price); payload.stockQuantity = Number(payload.stockQuantity); payload.categoryId = Number(payload.categoryId); payload.active = true; await request('/api/admin/products', { method: 'POST', body: JSON.stringify(payload) }); event.currentTarget.reset(); await loadCatalog(); await loadAdmin(); toast('Product created'); }

$('catalogTab').onclick = () => setView('shop'); $('adminTab').onclick = () => setView('admin'); $('refreshButton').onclick = () => loadCatalog().catch(showError); $('reloadAdminButton').onclick = () => loadAdmin().catch(showError); $('cartButton').onclick = () => { $('cartDrawer').classList.add('open'); loadCart().catch(showError); }; $('closeCart').onclick = () => $('cartDrawer').classList.remove('open'); $('authButton').onclick = () => state.user ? logout() : openAuth(); $('closeAuth').onclick = closeAuth; $('toggleAuth').onclick = () => setAuthMode(state.authMode === 'login' ? 'register' : 'login'); $('authForm').onsubmit = (event) => submitAuth(event).catch(showError); $('checkoutForm').onsubmit = (event) => checkout(event).catch(showError); $('payLatestButton').onclick = () => payLatest().catch(showError); $('categoryForm').onsubmit = (event) => createCategory(event).catch(showError); $('productForm').onsubmit = (event) => createProduct(event).catch(showError); $('searchInput').oninput = async (event) => { const q = event.target.value.trim(); renderProducts(q ? await request('/api/products/search?q=' + encodeURIComponent(q)) : state.products); }; if ($('passwordField')) $('passwordField').oninput = () => { if (state.authMode === 'register') setPasswordHelp(isStrongPassword($('passwordField').value)); };
document.querySelectorAll('.demo-users button').forEach((button) => button.onclick = async () => { const auth = await request('/api/auth/login', { method: 'POST', body: JSON.stringify({ email: button.dataset.email, password: button.dataset.password, role: button.dataset.role }) }); saveSession(auth); closeAuth(); await loadCart(); toast('Signed in as ' + auth.role.toLowerCase()); });
renderAuth(); setAuthMode('login'); setView('shop'); loadCatalog().then(loadCart).catch(showError);
